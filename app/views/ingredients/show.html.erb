<p style="color: green"><%= notice %></p>

<%= render @ingredient %>

<div>
  <%= link_to "Edit this ingredient", edit_ingredient_path(@ingredient) %> |
  <%= link_to "Voltar para ingredientes", ingredients_path %>

  <%= button_to "Excluir este ingrediente", @ingredient, method: :delete, name: "destroy_ingredient" %>
</div>
